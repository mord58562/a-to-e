# Resume notes — 2026-05-25 session pause

Hit session/weekly limit mid-flight. Resets 11 am Sydney. None of these commits are pushed.

## Local commits (in chronological order, all unpushed)

Run `git log --oneline origin/main..HEAD` to see them. Expected set:

- `a415cf8` cloudflare-worker: Argon2id, AES-GCM email envelope, hashed session tokens, brute-force rate-limit, defense-in-depth headers
- `852547c` paeds main file audit (24 Qs) — croup mechanism rationale strengthened, pyloric dx-leak → `paeds-009-r2`
- `8adeece` O&G audit — atosiban dropped 13→7 (all remaining flag non-availability), Sheehan + ectopic-MTX lead-ins rewritten, FIGO fibroid dx-leak, BP-contradiction PPH, 7 grammar fixes, 6 `-r2` revisions
- `bd7af1e` Psych + Medicine audit — 13+5 telegraph rewrites, 5+4 length pads, 7 non-NSW locations → HNE LHD
- `ee11089` … `4a78f0c` UI/UX 21-fix series — login-flash pre-paint, post-login render fallback, stethoscope refinish, AN/STAT wired, ref-panel toggle, +glucose conditional, WASD + arrow + Esc keyboard nav, scroll-after-submit, droopy curve flattened, cursor:text on metadata, hover-card glossary (review-only, AN toggle), webkit-app-region:no-drag, softer light theme, Mark→Flag, empty Ix hidden, `/api/history` endpoint for cross-device Unseen
- Interleaved batch-generation commits from D1 (psych) — already in place

## Push order when ready

```
cd ~/y4-mcq && git log --oneline origin/main..HEAD     # sanity check
cd ~/y4-mcq && git push origin main                    # site + worker source
```

## Worker deploy (separate, MANUAL — needs secrets)

The `/api/history` endpoint (Unseen filter cross-device) + Argon2id upgrade requires:

```
cd ~/y4-mcq/cloudflare-worker
npm install
# generate 3 secrets via `openssl rand -base64 32` then:
wrangler secret put EMAIL_HMAC_KEY
wrangler secret put EMAIL_ENC_KEY
wrangler secret put SESSION_PEPPER
wrangler d1 execute a-to-e --file=schema_002_encrypt.sql --remote
wrangler deploy
```

See `cloudflare-worker/MIGRATION_2026-05-25.md` for verification queries.

Existing PBKDF2 accounts auto-migrate to Argon2id on next login (lazy migration).

## Still-pending work (deferred to next session)

1. **Paeds-batches audit completion** — agent was mid-run when limit hit. The cited paeds fixes (pertussis admit-telegraph, measles PEP option mismatch, status epi cutoff, febrile neutropenia "omitting empirical") live in batch files. Re-launch with: `Agent` C1b prompt.
2. **Medicine batch generation** — both medicine generation agents failed (1 stalled, 1 hit limit). 0 new medicine batches written this session. Re-launch with smaller per-agent scope (1 batch each).
3. **vEDS In Context mechanism rewrite** — lives in `paeds_genetic_syndromes.json`; out of obgyn-audit scope. Pick up in paeds-batches audit re-run.
4. **Meta count refresh** — `data/meta.json` `total_questions` and `by_topic` are stale relative to actual file contents.
5. **Side requests not yet addressed**:
   - "essentially all of your tokens for the rest of my subscription are going to be dedicated to this project" — i.e. keep generating Psych + Medicine to parity (target ≥250 each).
   - Be creative with new question types testing clinical reasoning in unusual ways.
   - 90% session-limit pause rule — honoured this session.

## Desktop cleanup approvals needed

Recon-only completed. Rob's call on these 9 safe-to-remove items:

- `~/Desktop/.DS_Store`, `.localized`, `.Screenshot 2025-{08,09,11}-*.png` (3 old screenshots)
- `~/Desktop/desktop.ini`
- `~/Desktop/sync_cleanup.sh`, `sync_cleanup.log`
- `~/Desktop/marquee-test-install.txt`

Keep: `~/Desktop/claude omptpray/`, `indigenous_suicide_research_answers.txt`, `tax/`.

## Quick health-check before push

```
cd ~/y4-mcq
git status                                             # working tree clean
git log --oneline origin/main..HEAD | wc -l            # commit count to push
python3 -c "
import json, glob
for f in ['questions_paeds.json','questions_obgyn.json','questions_psych.json','questions_medicine.json']:
    try: print(f, len(json.load(open(f'data/{f}'))))
    except: print(f, 'missing')
print('batches:', len(glob.glob('data/batches/*.json')))
"
```

If counts look right and `git status` is clean, push.
